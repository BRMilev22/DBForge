PORT=4000

DBFORGE_PG_HOST=your-postgres-host
DBFORGE_PG_PORT=10020
DBFORGE_PG_USERNAME=postgres-user
DBFORGE_PG_PASSWORD=postgres-password
DBFORGE_PG_DATABASE=postgres_db

# Optional DBForge API token if you want to use the proxy mode instead of direct credentials.
DBFORGE_API_TOKEN=
