# DBForge Production Environment Variables
# Copy this to .env.production and update values

# API URL - should be the domain with /api path
# Nginx will proxy /api requests to backend on port 8080
VITE_API_URL=https://dbforge.dev/api

# For local development, use:
# VITE_API_URL=http://localhost:8080/api
